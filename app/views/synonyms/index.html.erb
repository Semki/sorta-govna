<h1>Список синонимов</h1>

<table>
  <tr>
    <th>Название</th>
    <th>Концепт</th>
	<th>Релевантность</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @synonyms.each do |synonym| %>
  <tr>
    <td><%= synonym.name %> </td>
    <td><%= link_to concept_in_list(synonym.shit), synonym.shit %></td>
    <td><%= synonym.relevance %></td>
	<td><%= link_to 'Show', synonym %></td>
    <td><%= link_to 'Edit', edit_synonym_path(synonym) %></td>
    <td><%= link_to 'Destroy', synonym, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Synonym', new_synonym_path %>
